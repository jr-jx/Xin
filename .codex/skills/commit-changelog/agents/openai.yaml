interface:
  display_name: "Commit Changelog"
  short_description: "Record logs before AI-created commits"
  default_prompt: "Use $commit-changelog when the user asks the AI agent to commit Xin changes, appending concise change log entries to AGENT.md and CLAUDE.md before git commit."

policy:
  allow_implicit_invocation: true
